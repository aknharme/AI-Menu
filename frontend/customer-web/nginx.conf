server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # QR ile acilan tum route'lar SPA fallback ile ayni entry'ye yonlenir.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
